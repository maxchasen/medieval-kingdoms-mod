pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

// Gradle project name (artifact / IDE); mod id is in fabric.mod.json
rootProject.name = 'medieval-kingdoms'
